{
  "packages": [
    "packages/*"
  ],
  "useWorkspaces": true,
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "publish": {
      "registry": "http://localhost:4873/",
      "ignoreChanges": [
        "ignored-file",
        "*.md"
      ]
    }
  }
}
